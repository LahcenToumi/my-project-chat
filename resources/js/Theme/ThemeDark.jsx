

import React from 'react';
import '../../css/assets/css/template.bundle.dark.css';

const Dark_Mode = ({ children }) => {
  // Apply dark styles to the body element
  

  // Render the children inside a wrapper div
  return <>{children}</>;
};

export default Dark_Mode;